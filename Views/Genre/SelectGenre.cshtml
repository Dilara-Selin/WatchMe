@model IEnumerable<WatchMe.Models.Genre>

<h2>Select Genre</h2>

<ul>
    @foreach (var genre in Model)
    {
        <li>
            <a href="@Url.Action("MoviesByGenre", "Genre", new { genreId = genre.GenreId })">
                @genre.Name
            </a>
        </li>
    }
</ul>
